import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cloud.konkamon.ionic',
  appName: 'cloud-konkamon-ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
